
#user-list.content-block
  .content-block-header
    %h2= @institution.name
    %h3= t('user.index.user_list.header_institutions', :count => @users.size)

  .content-block-middle
    - for user in @users
      = render :partial => 'users/detailed_user_institution', :locals => { :user => user }
